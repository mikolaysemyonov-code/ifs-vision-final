# As-Built Technical Report

**Продукт:** IFS Vision — интеллектуальный финансовый симулятор недвижимости (White Label)  
**Дата отчёта:** 21.02.2026  
**Цель:** Финальная техническая документация для оценки коммерческой готовности к запуску на Vercel.

---

## 1. Интеграция с Telegram (отправка лидов)

### Реализация

- **Роут:** `POST /api/telegram/send`  
  Файл: `src/app/api/telegram/send/route.ts`

### Передаваемые данные

| Поле          | Тип    | Источник | Описание |
|---------------|--------|----------|----------|
| `name`        | string | Форма в модалке | Имя клиента (опционально) |
| `phone`       | string | Форма в модалке | Телефон (опционально) |
| `strategy`    | string | Zustand `currentScenario` | Человекочитаемое название: «Инвестиция», «Семья», «Старт» или «Текущий сценарий» |
| `roi`         | number | `FinancialEngine.roi()` → `roiPercent` | Итоговый ROI в % (годовая доходность на вложенный капитал) |
| `companyName` | string | `config.brand.companyName` (из админки) | Название продукта в тексте сообщения |

### Формат сообщения в Telegram

```
🔥 Новый расчёт в {companyName}!

📊 Стратегия: {strategy}
📈 ROI: {roi}%

👤 Имя: {name или "—"}
📞 Тел: {phone или "—"}
```

### Использование переменных окружения

- **`process.env.BOT_TOKEN`** — токен бота (Telegram Bot API). При отсутствии или пустом значении роут возвращает **500** и не вызывает Telegram.
- **`process.env.CHAT_ID`** — ID чата/канала для отправки. При отсутствии — **500**.

Секреты **не захардкожены** в коде, только чтение из `process.env`.

### Точка вызова

- Кнопка **«Получить детальный план в PDF»** на главной странице открывает модальное окно с полями «Имя» и «Телефон».
- При нажатии **«Отправить и скачать PDF»** выполняется `POST /api/telegram/send` с текущими стратегией, ROI и контактами, затем запускается генерация и скачивание PDF.
- Опция **«Скачать без отправки»** только скачивает PDF, без вызова API.

---

## 2. Система безопасности (Auth)

### Текущий статус

| Компонент | Статус | Детали |
|-----------|--------|--------|
| Защита `/studio-admin` | Реализована | Middleware проверяет cookie `admin_session` для всех путей, начинающихся с `/studio-admin`, кроме `/studio-admin/login`. |
| Middleware | Включён | `src/middleware.ts`, matcher: `["/studio-admin", "/studio-admin/:path*"]`. |
| Редирект при отсутствии cookie | Реализован | Редирект на **`/studio-admin/login`** (307). |
| Вход по паролю | Реализован | `POST /api/login`: пароль только из **`process.env.ADMIN_PASSWORD`**. При успехе выставляется HTTP-only cookie `admin_session` (path=/, maxAge=1h, sameSite=lax, secure в production). |
| Проверка пароля при смене в админке | Реализована | `POST /api/verify-password` использует **`process.env.ADMIN_PASSWORD`**. |
| 2FA через Telegram | Не реализована | Отдельного роута `/api/verify-2fa` или логики двухфакторной аутентификации в API **нет**. Вход только по паролю. |

### Итог по безопасности

- Админка защищена cookie-сессией и middleware.
- Пароль хранится только в переменных окружения, в коде не захардкожен.
- 2FA (в т.ч. через Telegram) в текущей сборке **не реализована**.

---

## 3. Валютная логика и курсы

### Роут `/api/rates`

- **Метод:** GET  
- **Файл:** `src/app/api/rates/route.ts`

**Поведение:**

- Запрос к внешнему API: `https://api.exchangerate-api.com/v4/latest/USD`.
- Возвращаемый формат: `{ USD: number, AED: number }` — сколько рублей за 1 USD и за 1 AED.
- **Кэширование:** in-memory кэш на **1 час** (CACHE_TTL_MS = 60 * 60 * 1000). При повторном запросе в течение часа возвращаются закэшированные данные без вызова внешнего API.
- **Принудительное обновление:** query-параметр `?revalidate=1` сбрасывает кэш и запрашивает свежие курсы (используется кнопкой «Обновить сейчас» в админке).
- **Fallback при ошибке:** при недоступности API или невалидном ответе возвращаются `cache?.data ?? FALLBACK` (FALLBACK = `{ USD: 90, AED: 25 }`). Приложение не падает.

### USDT и комиссия

- В Zustand store (`src/store/useStore.ts`) курс USDT к рублю считается как:  
  **`rates.USD * (1 + usdtFeePercent / 100)`**.
- `usdtFeePercent` задаётся в админке (Экономика → «Комиссия USDT к USD, %», 0–10). По умолчанию 0 (1:1 к USD).
- Конфиг валют строится в `getCurrencyConfigs(s, liveRates?)`: при переданных `liveRates` используются они, иначе `s.currencyRates` из админки или дефолт `{ USD: 90, AED: 25 }`.

### Синхронизация с Zustand

- **`exchangeRates`** — поле в store (`null` или `{ USD, AED }`). Заполняется только при включённом в админке **«Автообновление курсов»** (`ratesAutoUpdate: true`).
- **`fetchRates(opts?)`** — запрос к `/api/rates` (или `/api/rates?revalidate=1`). При успехе и `getAdminSettings().ratesAutoUpdate === true` обновляет `exchangeRates` и пересчитывает `currencyConfigs` через `getCurrencyConfigs(s, data)`.
- При загрузке главной страницы, если `ratesAutoUpdate === true`, в `useEffect` вызывается `fetchRates()`.
- При сохранении настроек в админке диспатчится событие `ifsVisionAdminUpdated`; на главной вызывается `loadConfigFromStorage()`, который перечитывает `getAdminSettings()` и обновляет `config`, `currencyConfigs`, `currency`, `locale`. Курсы при выключенном автообновлении берутся из `currencyRates` в localStorage (ручной ввод или «Обновить сейчас» в админке).

Итог: курсы и USDT-комиссия согласованы с Zustand и админкой; кэш и fallback работают как задумано.

---

## 4. White Label и PDF (брендинг в скриншоте)

### Передача параметров брендинга в URL для Screenshot API

При формировании ссылки для генерации PDF (в `handleDownloadPDF`, `src/app/page.tsx`) в query добавляются (через **`encodeURIComponent`**):

| Параметр      | Значение              | Описание |
|---------------|------------------------|----------|
| `accentColor` | `brand.primaryColor`   | Цвет акцента (hex) |
| `companyName` | `brand.companyName`   | Название компании |
| `logoUrl`     | `brand.logoUrl` или `""` | URL логотипа |

Дополнительно в URL передаются: `export=1`, `ts`, `showTs`, `compare`, `locale`, `currency`.

### Подхват параметров на странице экспорта

- При открытии страницы с `export=1` и наличием хотя бы одного из `accentColor`, `companyName`, `logoUrl` вызывается **`parseExportBrandFromUrl(searchParams)`**, который декодирует значения через **`decodeURIComponent`** и возвращает объект брендинга (или `null`).
- **`effectiveBrand`** на экспорт-странице: если есть данные из URL — используются они (с подстановкой дефолтов при пустых полях), иначе — бренд из store.
- Применение на странице:
  - **Название в шапке:** `effectiveBrand.productName`.
  - **Подпись внизу отчёта:** «Официальный расчёт {effectiveBrand.companyName}. Сформировано …».
  - **Акцентный цвет:** на корневой обёртке задаётся CSS-переменная **`--accent-color`**; фоновый градиент строится через **`hexToRgba(effectiveBrand.primaryColor, 0.15)`** в `radial-gradient`, так что Screenshot API фиксирует кастомный цвет.
- **logoUrl** передаётся и парсится; при наличии элемента логотипа на странице экспорта его можно выводить из `effectiveBrand.logoUrl`.

Итог: брендинг (цвет, название компании, лого) передаётся в URL и применяется на странице экспорта; скриншот отражает настройки из админки.

---

## 5. Готовность к Vercel (Environment Variables)

### Список переменных окружения

| Переменная | Где используется | Обязательность | Назначение |
|-----------|------------------|----------------|------------|
| **ADMIN_PASSWORD** | `src/app/api/login/route.ts`, `src/app/api/verify-password/route.ts` | Обязательно для админки | Пароль входа в `/studio-admin` |
| **BOT_TOKEN** | `src/app/api/telegram/send/route.ts` | Обязательно для отправки лидов | Токен Telegram-бота |
| **CHAT_ID** | `src/app/api/telegram/send/route.ts` | Обязательно для отправки лидов | ID чата/канала для лидов |
| **SCREENSHOT_API_KEY** | `src/app/api/pdf/route.ts` | Обязательно для PDF | Ключ API Screenshot One |
| **NEXT_PUBLIC_SITE_URL** | `src/app/layout.tsx`, `src/app/api/pdf/route.ts` | Рекомендуется | URL сайта (metadata, подстановка localhost при PDF) |

### Проверка выноса секретов из кода

- В коде **нет** захардкоженных паролей, токенов бота, chat ID или API-ключей скриншота.
- Все чувствительные значения читаются только из **`process.env`**; при их отсутствии API возвращают явные ошибки (500/401), а не «тихий» fallback с дефолтным ключом.

Итог: для продакшена на Vercel достаточно задать перечисленные переменные в настройках проекта.

---

## 6. Дизайн-система

### Шрифты

- **Geist Sans** и **Geist Mono** (Next.js `next/font/google`).  
  Переменные: `--font-geist-sans`, `--font-geist-mono`. В `globals.css` в `@theme inline` заданы `--font-sans` и `--font-mono`.  
  Файл: `src/app/layout.tsx`.

### Tailwind

- **Tailwind CSS v4** (`"tailwindcss": "^4"`, `"@tailwindcss/postcss": "^4"` в `package.json`).  
  Подключение: `@import "tailwindcss"` и `@theme inline` в `src/app/globals.css`.

### Скругления и стеклянный стиль (Glassmorphism)

- **Скругления:** повсеместно используются **`rounded-3xl`** (и производные `rounded-xl`, `rounded-2xl`) для карточек, кнопок, модалок, полей ввода.  
  Примеры: главная страница, админка, форма входа, модалка лида.
- **Стеклянный эффект:** комбинация **`backdrop-blur-xl`**, **`bg-white/5`**, **`border border-white/10`** (в т.ч. класс `glassClass`: `rounded-3xl border border-white/10 bg-white/5 backdrop-blur-xl`) для панелей и карточек.  
  Фон страницы — тёмный (`bg-[#0c0c0e]`), градиент и полупрозрачные блоки создают эффект glassmorphism.

Подтверждение: дизайн-система построена на Geist, Tailwind v4, скруглениях 3xl и стеклянном стиле.

---

## 7. Сводка коммерческой готовности

| Область | Статус | Комментарий |
|---------|--------|-------------|
| Telegram-лиды | Готово | Роут реализован, данные (ROI, стратегия, контакты) передаются, BOT_TOKEN и CHAT_ID из env. |
| Безопасность админки | Готово | Middleware + cookie, пароль из env. 2FA не реализована. |
| Валюты и курсы | Готово | /api/rates с кэшем и fallback, USDT с комиссией, синхронизация с Zustand и админкой. |
| White Label в PDF | Готово | Параметры брендинга в URL, подхват на странице экспорта (цвет, название, лого). |
| Секреты и Vercel | Готово | Все секреты в process.env, список env-переменных определён. |
| Дизайн-система | Готово | Geist, Tailwind v4, rounded-3xl, glassmorphism. |

**Рекомендация перед запуском:** задать в Vercel переменные **ADMIN_PASSWORD**, **BOT_TOKEN**, **CHAT_ID**, **SCREENSHOT_API_KEY** и при необходимости **NEXT_PUBLIC_SITE_URL**. При необходимости 2FA — реализовать отдельно (отдельный роут и сценарий входа).
